================================================================================
BYZANTINE-ROBUST IDENTITY FRAMEWORK - REQUIREMENTS
================================================================================

REQUIREMENTS IMPROVEMENTS:
--------------------------

PREVIOUS: Basic simulation with toy arrays
NEW: Full production-ready ML pipeline with Byzantine defense

CORE ML LIBRARIES (NEW):
  numpy>=1.21.0           : Numerical computing, array operations
  pandas>=1.3.0            : Data manipulation, CSV logging
  scikit-learn>=1.0.0      : RandomForest, preprocessing, metrics
  xgboost>=1.5.0           : XGBoost classifier (optional, RF fallback)

BLOCKCHAIN (NEW):
  web3>=6.0.0              : Ethereum/web3.py integration

VISUALIZATION (NEW):
  plotly>=5.0.0            : Interactive charts for dashboard
  matplotlib>=3.5.0        : Plotting utilities
  streamlit>=1.0.0         : Dashboard web app framework

REPORTING (NEW):
  reportlab>=4.0.0         : PDF generation (optional)

TESTING (NEW):
  pytest>=7.0.0            : Unit testing framework
  pytest-cov>=3.0.0        : Coverage reporting

DEVELOPMENT:
  black>=22.0.0            : Code formatting
  flake8>=4.0.0            : Linting

INSTALLATION:
-------------
pip install -r requirements.txt

MINIMAL INSTALL (Core only):
pip install numpy pandas scikit-learn

FULL INSTALL (Everything):
pip install -r requirements.txt

OPTIONAL COMPONENTS:
-------------------
- XGBoost: pip install xgboost (falls back to RandomForest if missing)
- Blockchain: pip install web3 (runs in simulation mode if missing)
- Dashboard: pip install streamlit plotly
- PDF Reports: pip install reportlab (HTML fallback if missing)

VERIFICATION:
------------
python main.py --mode baseline --rounds 3
python main.py --mode reputation

================================================================================
END OF REQUIREMENTS
================================================================================
